# MeT3_R Beispiele

## Installation

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r .\requirements.txt  
python -m ipykernel install --user --name=met3_r --display-name "MeT3_R"
```